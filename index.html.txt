<!doctype html>
<html lang="tr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Günün Tavsiyesi</title>
  <style>
    /* Masum içerik stili */
    body { font-family: Arial, Helvetica, sans-serif; margin:0; padding:40px; background:#f7f7fb; color:#222; }
    .card { max-width:700px; margin:40px auto; background:white; padding:24px; border-radius:12px;
           box-shadow:0 6px 20px rgba(0,0,0,0.06); text-align:left; }
    h1 { margin:0 0 8px; }
    p { color:#555; line-height:1.5; }

    /* Gizli büyük uyarı örtüsü (başlangıçta görünmez) */
    #prank {
      position:fixed; inset:0; display:flex; align-items:center; justify-content:center;
      background:rgba(0,0,0,0.8); color:white; font-size:40px; font-weight:700;
      z-index:9999; opacity:0; pointer-events:none; transition:opacity .25s ease;
      text-align:center; padding:20px;
    }
    #prank.show { opacity:1; pointer-events:auto; }

    /* Kapat butonu */
    #closeBtn {
      margin-top:18px; display:inline-block; padding:8px 14px; border-radius:8px;
      background:rgba(255,255,255,0.12); cursor:pointer; font-size:16px;
    }
  </style>
</head>
<body>
  <div class="card">
    <h1>Bugün için küçük bir tavsiye</h1>
    <p>Bu sayfa masum bir bilgi gösteriyor. Güzel şeyler oku. Kahveni al, rahatla.</p>
    <p>Arada sırada ufak sürprizler iyidir. 😉</p>
  </div>

  <div id="prank" role="dialog" aria-hidden="true">
    <div>
      <div style="font-size:56px;letter-spacing:2px;">FEMBOYSUN</div>
      <div id="closeBtn">Kapat</div>
    </div>
  </div>

  <script>
    // Basit tetikleyiciler — açınca değil, 1.5s sonra ya da tıklayınca göster.
    // Kullanıcı açınca "masum" görünüyor; sonra sürpriz beliriyor.
    setTimeout(()=> showPrank(), 1500);

    // Alternatif: kullanıcı sayfada tıklayınca da açılmasını istersen:
    // document.addEventListener('click', ()=> showPrank());

    function showPrank(){
      const el = document.getElementById('prank');
      el.classList.add('show');
      el.setAttribute('aria-hidden','false');
    }

    // Kapat butonu
    document.getElementById('closeBtn').addEventListener('click', ()=>{
      const el = document.getElementById('prank');
      el.classList.remove('show');
      el.setAttribute('aria-hidden','true');
    });

    // Esc ile kapatma
    document.addEventListener('keydown', (e)=>{
      if(e.key === 'Escape'){
        const el = document.getElementById('prank');
        el.classList.remove('show');
        el.setAttribute('aria-hidden','true');
      }
    });
  </script>
</body>
</html>
